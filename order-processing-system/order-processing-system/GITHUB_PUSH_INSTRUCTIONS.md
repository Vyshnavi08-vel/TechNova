Steps to push this workspace to https://github.com/Vyshnavi08-vel/TechNova

1. Open a terminal in the project root: `c:\Users\vyshn\Downloads\order-processing-system\order-processing-system`
2. Make sure `git` is installed and you're authenticated with GitHub (SSH or HTTPS credentials).
3. Run one of the helper scripts:

- Bash/macOS/Linux

```bash
./push-to-github.sh
```

- PowerShell (Windows)

```powershell
.\push-to-github.ps1
```

4. If GitHub rejects the push due to an existing repo with different history, run `git push -u origin main --force` after reviewing.

5. Verify on GitHub that the repository `Vyshnavi08-vel/TechNova` contains your project files.
